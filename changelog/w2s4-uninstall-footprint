Significance: minor
Type: added

Added a footprint-manifest uninstall story: `includes/_uninstall-footprint.php` lists every
wp_options row and user-meta key the plugin owns (currently none), and the new `uninstall.php`
deletes them directly, framework-free, in WordPress's cold uninstall bootstrap.
